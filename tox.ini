[tox]
env_list = py39, py310, py311, py312, py313, py314
isolated_build = true
requires =
    tox-uv>=1.16

[testenv]
description = Verify package builds and imports on supported Python versions
commands =
    python --version
    python -c "import photonicdrivers"
